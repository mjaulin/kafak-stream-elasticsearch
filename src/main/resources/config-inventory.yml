server:
  type: simple
  applicationContextPath: /
  adminContextPath: /admin
  connector:
    type: http
    port: 8882

logging:
  level: WARN
  loggers:
    "org.example":
      level: DEBUG
      additive: false
      appenders:
        - type: console
  appenders:
    - type: console
